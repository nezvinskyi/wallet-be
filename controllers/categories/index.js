const addCategory = require('./add-category.ctrl');
const delCategory = require('./add-category.ctrl');
const getAll = require('./getAll');

module.exports = {
  addCategory,
  delCategory,
  getAll,
};
